[package]
name = "riscv-test"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]
resolver = "2"

[features]
float = ["portable-atomic/float"]

[dependencies]
portable-atomic = { path = "../.." }
test-helper = { path = "../helper" }

fdt = "0.1"
paste = "1"
