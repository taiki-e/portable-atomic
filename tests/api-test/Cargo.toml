[package]
name = "api-test"
version = "0.0.0"
edition = "2018"
rust-version = "1.34"
publish = false

[lib]
doctest = false
doc = false

[features]
float = ["portable-atomic/float"]

[dependencies]
portable-atomic = { path = "../.." }
test-helper = { path = "../helper" }

paste = "1"
