asm_test::atomic_u128_load_intrinsics::relaxed:
 push    rbx
 xor     eax, eax
 xor     edx, edx
 xor     ecx, ecx
 xor     ebx, ebx
 lock    cmpxchg16b, xmmword, ptr, [rdi]
 pop     rbx
 ret
asm_test::atomic_u128_load_intrinsics::acquire:
 push    rbx
 xor     eax, eax
 xor     edx, edx
 xor     ecx, ecx
 xor     ebx, ebx
 lock    cmpxchg16b, xmmword, ptr, [rdi]
 pop     rbx
 ret
asm_test::atomic_u128_load_intrinsics::seq_cst:
 push    rbx
 xor     eax, eax
 xor     edx, edx
 xor     ecx, ecx
 xor     ebx, ebx
 lock    cmpxchg16b, xmmword, ptr, [rdi]
 pop     rbx
 ret
