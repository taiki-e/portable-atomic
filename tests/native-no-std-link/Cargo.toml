[package]
name = "native-no-std-link-test"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]
resolver = "2"

[dependencies]
cfg-if = "1"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
